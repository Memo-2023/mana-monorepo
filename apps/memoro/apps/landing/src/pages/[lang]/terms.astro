---
export async function getStaticPaths() {
  return ['de', 'en'].map(lang => ({
    params: { lang },
    props: { lang }
  }));
}

const { lang } = Astro.props;

// Redirect to new location
return Astro.redirect(`/${lang}/privacy/terms`, 301);
---