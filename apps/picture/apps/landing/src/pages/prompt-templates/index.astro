---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import {
	getAllPromptTemplates,
	getFeaturedTemplates,
	getAllCategories,
	getAllTags,
	getTemplateStats,
	formatCategoryName,
	getDifficultyColor,
} from '../../utils/promptTemplates';
import { t } from '../../i18n';

const allTemplates = await getAllPromptTemplates();
const featuredTemplates = await getFeaturedTemplates(6);
const categories = await getAllCategories();
const tags = await getAllTags();
const stats = await getTemplateStats();

// Get unique difficulty levels
const difficulties = ['beginner', 'intermediate', 'advanced'];

// Get unique models
const allModels = [
	...new Set(allTemplates.map((t) => t.data.recommendedModel)),
].sort();
---

<Layout
	title="AI Prompt Templates - Picture AI Image Generator"
	description="Browse our collection of professional prompt templates for AI image generation. Create stunning images faster with pre-built prompts for FLUX, Ideogram, and more."
>
	<!-- Hero Section -->
	<section class="bg-gradient-to-br from-purple-900 via-purple-800 to-indigo-900 text-white py-20">
		<div class="container mx-auto px-4">
			<div class="max-w-4xl mx-auto text-center">
				<h1 class="text-5xl md:text-6xl font-bold mb-6">
					AI Prompt Templates
				</h1>
				<p class="text-xl md:text-2xl mb-8 text-purple-100">
					Professional prompt templates for stunning AI images. Save time and get
					better results with our curated collection.
				</p>

				<!-- Stats -->
				<div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12">
					<div class="bg-white/10 backdrop-blur-sm rounded-lg p-6">
						<div class="text-3xl font-bold">{stats.total}</div>
						<div class="text-purple-200">Templates</div>
					</div>
					<div class="bg-white/10 backdrop-blur-sm rounded-lg p-6">
						<div class="text-3xl font-bold">{stats.totalUses.toLocaleString()}</div>
						<div class="text-purple-200">Uses</div>
					</div>
					<div class="bg-white/10 backdrop-blur-sm rounded-lg p-6">
						<div class="text-3xl font-bold">{categories.length}</div>
						<div class="text-purple-200">Categories</div>
					</div>
					<div class="bg-white/10 backdrop-blur-sm rounded-lg p-6">
						<div class="text-3xl font-bold">{stats.averageRating}</div>
						<div class="text-purple-200">Avg Rating</div>
					</div>
				</div>

				<!-- Search Bar -->
				<div class="max-w-2xl mx-auto">
					<div class="relative">
						<input
							type="text"
							id="search-input"
							placeholder="Search templates..."
							class="w-full px-6 py-4 rounded-full text-gray-900 text-lg focus:outline-none focus:ring-4 focus:ring-purple-300"
						/>
						<button
							class="absolute right-2 top-1/2 -translate-y-1/2 bg-purple-600 hover:bg-purple-700 text-white px-8 py-2 rounded-full font-semibold transition-colors"
						>
							Search
						</button>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Filters Section -->
	<section class="bg-gray-50 border-b sticky top-0 z-10">
		<div class="container mx-auto px-4 py-6">
			<div class="flex flex-wrap gap-4 items-center">
				<!-- Category Filter -->
				<div class="flex-1 min-w-[200px]">
					<label for="category-filter" class="block text-sm font-medium text-gray-700 mb-1">
						Category
					</label>
					<select
						id="category-filter"
						class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
					>
						<option value="">All Categories</option>
						{
							categories.map((cat) => (
								<option value={cat.category}>
									{cat.icon} {formatCategoryName(cat.category)} ({cat.count})
								</option>
							))
						}
					</select>
				</div>

				<!-- Difficulty Filter -->
				<div class="flex-1 min-w-[200px]">
					<label for="difficulty-filter" class="block text-sm font-medium text-gray-700 mb-1">
						Difficulty
					</label>
					<select
						id="difficulty-filter"
						class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
					>
						<option value="">All Levels</option>
						{
							difficulties.map((diff) => (
								<option value={diff}>
									{diff.charAt(0).toUpperCase() + diff.slice(1)} ({stats.byDifficulty[diff as keyof typeof stats.byDifficulty]})
								</option>
							))
						}
					</select>
				</div>

				<!-- Model Filter -->
				<div class="flex-1 min-w-[200px]">
					<label for="model-filter" class="block text-sm font-medium text-gray-700 mb-1">
						Model
					</label>
					<select
						id="model-filter"
						class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
					>
						<option value="">All Models</option>
						{allModels.map((model) => <option value={model}>{model}</option>)}
					</select>
				</div>

				<!-- Sort -->
				<div class="flex-1 min-w-[200px]">
					<label for="sort-select" class="block text-sm font-medium text-gray-700 mb-1">
						Sort By
					</label>
					<select
						id="sort-select"
						class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent"
					>
						<option value="popular">Most Popular</option>
						<option value="recent">Most Recent</option>
						<option value="rating">Highest Rated</option>
						<option value="uses">Most Used</option>
					</select>
				</div>
			</div>

			<!-- Active Filters -->
			<div id="active-filters" class="flex flex-wrap gap-2 mt-4 hidden"></div>
		</div>
	</section>

	<!-- Featured Templates -->
	{
		featuredTemplates.length > 0 && (
			<section class="py-16 bg-white">
				<div class="container mx-auto px-4">
					<div class="flex items-center justify-between mb-8">
						<div>
							<h2 class="text-3xl font-bold text-gray-900">Featured Templates</h2>
							<p class="text-gray-600 mt-2">
								Hand-picked templates for the best results
							</p>
						</div>
					</div>

					<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
						{featuredTemplates.map((template) => (
							<a
								href={`/prompt-templates/${template.id}`}
								class="group bg-white border border-gray-200 rounded-xl overflow-hidden hover:shadow-xl transition-all duration-300 hover:-translate-y-1"
							>
								{/* Template Card */}
								<div class="p-6">
									{/* Header */}
									<div class="flex items-start justify-between mb-4">
										<div class="flex items-center gap-3">
											<span class="text-4xl">{template.data.icon}</span>
											<div>
												<h3 class="text-xl font-bold text-gray-900 group-hover:text-purple-600 transition-colors">
													{template.data.title}
												</h3>
												<div class="flex items-center gap-2 mt-1">
													<span
														class={`px-2 py-0.5 text-xs font-semibold rounded-full ${
															template.data.difficulty === 'beginner'
																? 'bg-green-100 text-green-800'
																: template.data.difficulty === 'intermediate'
																	? 'bg-yellow-100 text-yellow-800'
																	: 'bg-red-100 text-red-800'
														}`}
													>
														{template.data.difficulty}
													</span>
													{template.data.featured && (
														<span class="px-2 py-0.5 text-xs font-semibold rounded-full bg-purple-100 text-purple-800">
															Featured
														</span>
													)}
												</div>
											</div>
										</div>
									</div>

									{/* Description */}
									<p class="text-gray-600 mb-4 line-clamp-2">
										{template.data.description}
									</p>

									{/* Category & Tags */}
									<div class="flex flex-wrap gap-2 mb-4">
										<span class="px-3 py-1 bg-gray-100 text-gray-700 text-sm rounded-full">
											{formatCategoryName(template.data.category)}
										</span>
										{template.data.tags.slice(0, 2).map((tag) => (
											<span class="px-3 py-1 bg-gray-50 text-gray-600 text-sm rounded-full">
												#{tag}
											</span>
										))}
									</div>

									{/* Stats */}
									<div class="flex items-center justify-between pt-4 border-t border-gray-100">
										<div class="flex items-center gap-4 text-sm text-gray-500">
											<span class="flex items-center gap-1">
												<svg
													class="w-4 h-4"
													fill="none"
													stroke="currentColor"
													viewBox="0 0 24 24"
												>
													<path
														stroke-linecap="round"
														stroke-linejoin="round"
														stroke-width="2"
														d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"
													/>
												</svg>
												{template.data.likes}
											</span>
											<span class="flex items-center gap-1">
												<svg
													class="w-4 h-4"
													fill="none"
													stroke="currentColor"
													viewBox="0 0 24 24"
												>
													<path
														stroke-linecap="round"
														stroke-linejoin="round"
														stroke-width="2"
														d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"
													/>
													<path
														stroke-linecap="round"
														stroke-linejoin="round"
														stroke-width="2"
														d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"
													/>
												</svg>
												{template.data.uses}
											</span>
										</div>
										<div class="flex items-center gap-1 text-yellow-500">
											<svg
												class="w-4 h-4 fill-current"
												viewBox="0 0 24 24"
											>
												<path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
											</svg>
											<span class="text-sm font-semibold text-gray-700">
												{template.data.rating}
											</span>
										</div>
									</div>

									{/* Model */}
									<div class="mt-4 pt-4 border-t border-gray-100">
										<div class="flex items-center justify-between text-sm">
											<span class="text-gray-500">Recommended:</span>
											<span class="font-semibold text-gray-700">
												{template.data.recommendedModel}
											</span>
										</div>
									</div>
								</div>
							</a>
						))}
					</div>
				</div>
			</section>
		)
	}

	<!-- All Templates Grid -->
	<section class="py-16 bg-gray-50">
		<div class="container mx-auto px-4">
			<div class="flex items-center justify-between mb-8">
				<div>
					<h2 class="text-3xl font-bold text-gray-900">All Templates</h2>
					<p class="text-gray-600 mt-2" id="results-count">
						Showing {allTemplates.length} templates
					</p>
				</div>
			</div>

			<div
				id="templates-grid"
				class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6"
			>
				{
					allTemplates.map((template) => (
						<a
							href={`/prompt-templates/${template.slug}`}
							class="template-card group bg-white border border-gray-200 rounded-lg overflow-hidden hover:shadow-lg transition-all duration-300 hover:-translate-y-1"
							data-category={template.data.category}
							data-difficulty={template.data.difficulty}
							data-model={template.data.recommendedModel}
							data-tags={JSON.stringify(template.data.tags)}
							data-title={template.data.title.toLowerCase()}
							data-description={template.data.description.toLowerCase()}
							data-rating={template.data.rating}
							data-uses={template.data.uses}
							data-date={template.data.publishDate.getTime()}
						>
							<div class="p-5">
								{/* Icon & Badges */}
								<div class="flex items-start justify-between mb-3">
									<span class="text-3xl">{template.data.icon}</span>
									<div class="flex gap-1">
										{template.data.popular && (
											<span class="px-2 py-0.5 text-xs font-semibold rounded-full bg-blue-100 text-blue-800">
												Popular
											</span>
										)}
										{template.data.trending && (
											<span class="px-2 py-0.5 text-xs font-semibold rounded-full bg-orange-100 text-orange-800">
												🔥
											</span>
										)}
									</div>
								</div>

								{/* Title */}
								<h3 class="text-lg font-bold text-gray-900 group-hover:text-purple-600 transition-colors mb-2 line-clamp-2">
									{template.data.title}
								</h3>

								{/* Description */}
								<p class="text-sm text-gray-600 mb-3 line-clamp-2">
									{template.data.description}
								</p>

								{/* Difficulty */}
								<div class="mb-3">
									<span
										class={`px-2 py-1 text-xs font-semibold rounded-full ${
											template.data.difficulty === 'beginner'
												? 'bg-green-100 text-green-800'
												: template.data.difficulty === 'intermediate'
													? 'bg-yellow-100 text-yellow-800'
													: 'bg-red-100 text-red-800'
										}`}
									>
										{template.data.difficulty}
									</span>
								</div>

								{/* Stats */}
								<div class="flex items-center justify-between text-xs text-gray-500 mb-3">
									<span class="flex items-center gap-1">
										⭐ {template.data.rating}
									</span>
									<span>{template.data.uses} uses</span>
								</div>

								{/* Model */}
								<div class="pt-3 border-t border-gray-100">
									<span class="text-xs text-gray-500 font-medium">
										{template.data.recommendedModel}
									</span>
								</div>
							</div>
						</a>
					))
				}
			</div>

			<!-- No Results -->
			<div id="no-results" class="hidden text-center py-16">
				<div class="text-6xl mb-4">🔍</div>
				<h3 class="text-2xl font-bold text-gray-900 mb-2">No templates found</h3>
				<p class="text-gray-600">Try adjusting your filters or search query</p>
			</div>
		</div>
	</section>

	<!-- Popular Categories -->
	<section class="py-16 bg-white">
		<div class="container mx-auto px-4">
			<div class="text-center mb-12">
				<h2 class="text-3xl font-bold text-gray-900 mb-4">Browse by Category</h2>
				<p class="text-gray-600">Find the perfect template for your use case</p>
			</div>

			<div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
				{
					categories.map((cat) => (
						<button
							class="category-btn p-6 bg-gray-50 hover:bg-purple-50 border border-gray-200 hover:border-purple-300 rounded-xl transition-all duration-300 text-center group"
							data-category={cat.category}
						>
							<div class="text-4xl mb-2">{cat.icon}</div>
							<div class="font-semibold text-gray-900 group-hover:text-purple-600">
								{formatCategoryName(cat.category)}
							</div>
							<div class="text-sm text-gray-500 mt-1">{cat.count} templates</div>
						</button>
					))
				}
			</div>
		</div>
	</section>

	<!-- CTA Section -->
	<section class="py-20 bg-gradient-to-br from-purple-900 via-purple-800 to-indigo-900 text-white">
		<div class="container mx-auto px-4 text-center">
			<h2 class="text-4xl font-bold mb-6">Ready to Create Stunning Images?</h2>
			<p class="text-xl text-purple-100 mb-8 max-w-2xl mx-auto">
				Start using our prompt templates today and generate professional-quality AI
				images in seconds.
			</p>
			<div class="flex flex-col sm:flex-row gap-4 justify-center">
				<a
					href="/app"
					class="px-8 py-4 bg-white text-purple-600 font-semibold rounded-full hover:bg-purple-50 transition-colors"
				>
					Start Creating Free
				</a>
				<a
					href="/tutorials"
					class="px-8 py-4 bg-purple-600 text-white font-semibold rounded-full hover:bg-purple-700 transition-colors border-2 border-white"
				>
					Learn More
				</a>
			</div>
		</div>
	</section>

	<script>
		// Client-side filtering and sorting
		const searchInput = document.getElementById('search-input') as HTMLInputElement;
		const categoryFilter = document.getElementById('category-filter') as HTMLSelectElement;
		const difficultyFilter = document.getElementById(
			'difficulty-filter'
		) as HTMLSelectElement;
		const modelFilter = document.getElementById('model-filter') as HTMLSelectElement;
		const sortSelect = document.getElementById('sort-select') as HTMLSelectElement;
		const templatesGrid = document.getElementById('templates-grid') as HTMLElement;
		const noResults = document.getElementById('no-results') as HTMLElement;
		const resultsCount = document.getElementById('results-count') as HTMLElement;
		const activeFiltersContainer = document.getElementById('active-filters') as HTMLElement;
		const categoryButtons = document.querySelectorAll('.category-btn');

		let currentFilters = {
			search: '',
			category: '',
			difficulty: '',
			model: '',
			sort: 'popular',
		};

		function updateActiveFilters() {
			const filters: string[] = [];

			if (currentFilters.search) filters.push(`Search: "${currentFilters.search}"`);
			if (currentFilters.category) filters.push(`Category: ${currentFilters.category}`);
			if (currentFilters.difficulty)
				filters.push(`Difficulty: ${currentFilters.difficulty}`);
			if (currentFilters.model) filters.push(`Model: ${currentFilters.model}`);

			if (filters.length > 0) {
				activeFiltersContainer.innerHTML = filters
					.map(
						(filter) => `
					<span class="px-3 py-1 bg-purple-100 text-purple-800 rounded-full text-sm font-medium flex items-center gap-2">
						${filter}
						<button class="hover:text-purple-900" onclick="this.parentElement.remove(); filterTemplates();">×</button>
					</span>
				`
					)
					.join('');
				activeFiltersContainer.classList.remove('hidden');
			} else {
				activeFiltersContainer.classList.add('hidden');
			}
		}

		function filterTemplates() {
			const cards = Array.from(
				document.querySelectorAll('.template-card')
			) as HTMLElement[];

			let visibleCount = 0;

			cards.forEach((card) => {
				const matchesSearch =
					!currentFilters.search ||
					card.dataset.title?.includes(currentFilters.search.toLowerCase()) ||
					card.dataset.description?.includes(currentFilters.search.toLowerCase());

				const matchesCategory =
					!currentFilters.category || card.dataset.category === currentFilters.category;

				const matchesDifficulty =
					!currentFilters.difficulty ||
					card.dataset.difficulty === currentFilters.difficulty;

				const matchesModel =
					!currentFilters.model || card.dataset.model === currentFilters.model;

				if (matchesSearch && matchesCategory && matchesDifficulty && matchesModel) {
					card.style.display = 'block';
					visibleCount++;
				} else {
					card.style.display = 'none';
				}
			});

			// Sort visible cards
			const visibleCards = cards.filter((card) => card.style.display !== 'none');
			sortCards(visibleCards);

			// Update results count
			resultsCount.textContent = `Showing ${visibleCount} template${visibleCount !== 1 ? 's' : ''}`;

			// Show/hide no results
			if (visibleCount === 0) {
				noResults.classList.remove('hidden');
				templatesGrid.classList.add('hidden');
			} else {
				noResults.classList.add('hidden');
				templatesGrid.classList.remove('hidden');
			}

			updateActiveFilters();
		}

		function sortCards(cards: HTMLElement[]) {
			cards.sort((a, b) => {
				const sortBy = currentFilters.sort;

				if (sortBy === 'popular' || sortBy === 'uses') {
					return Number(b.dataset.uses) - Number(a.dataset.uses);
				} else if (sortBy === 'rating') {
					return Number(b.dataset.rating) - Number(a.dataset.rating);
				} else if (sortBy === 'recent') {
					return Number(b.dataset.date) - Number(a.dataset.date);
				}

				return 0;
			});

			cards.forEach((card) => templatesGrid.appendChild(card));
		}

		// Event listeners
		searchInput.addEventListener('input', (e) => {
			currentFilters.search = (e.target as HTMLInputElement).value;
			filterTemplates();
		});

		categoryFilter.addEventListener('change', (e) => {
			currentFilters.category = (e.target as HTMLSelectElement).value;
			filterTemplates();
		});

		difficultyFilter.addEventListener('change', (e) => {
			currentFilters.difficulty = (e.target as HTMLSelectElement).value;
			filterTemplates();
		});

		modelFilter.addEventListener('change', (e) => {
			currentFilters.model = (e.target as HTMLSelectElement).value;
			filterTemplates();
		});

		sortSelect.addEventListener('change', (e) => {
			currentFilters.sort = (e.target as HTMLSelectElement).value;
			filterTemplates();
		});

		// Category button clicks
		categoryButtons.forEach((btn) => {
			btn.addEventListener('click', () => {
				const category = (btn as HTMLElement).dataset.category || '';
				currentFilters.category = category;
				categoryFilter.value = category;
				filterTemplates();

				// Smooth scroll to templates grid
				templatesGrid.scrollIntoView({ behavior: 'smooth', block: 'start' });
			});
		});
	</script>
</Layout>
