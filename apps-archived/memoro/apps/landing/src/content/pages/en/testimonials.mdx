---
title: "Testimonials | Memoro"
description: "Discover what our customers say about Memoro"
lang: "en"
type: "page"
lastUpdated: 2024-02-22
sections:
  hero:
    title: "What Our Customers Say"
    subtitle: "Learn how Memoro helps teams and companies learn and work more efficiently"
  callToAction:
    title: "Ready for Better Meeting Documentation?"
    description: "Discover how Memoro makes your meetings more efficient and productive."
    buttonText: "Download App"
    buttonLink: "/en/download"
---

import TestimonialCard from "../../../components/TestimonialCard.astro";
import CallToAction from "../../../components/CallToAction.astro";

export const TestimonialsContent = ({ testimonials, lang }) => (
  <>
    <h1 class="text-4xl font-bold text-text-primary mb-8">
      {frontmatter.sections.hero.title}
    </h1>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {testimonials.map((testimonial) => (
        <TestimonialCard
          quote={testimonial.data.text}
          author={testimonial.data.name}
          role={testimonial.data.role}
          company={testimonial.data.company}
          image={testimonial.data.image}
          href={`/${lang}/testimonials/${testimonial.slug.replace(`${lang}/`, "")}`}
        />
      ))}
    </div>

    <div class="mt-16 text-center">
      <CallToAction
        title={frontmatter.sections.callToAction.title}
        description={frontmatter.sections.callToAction.description}
        buttonText={frontmatter.sections.callToAction.buttonText}
        buttonLink={frontmatter.sections.callToAction.buttonLink}
      />
    </div>

  </>
);

<TestimonialsContent {...props} />{" "}
